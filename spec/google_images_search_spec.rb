require 'spec_helper'

describe GoogleImagesSearch do
  it 'has a version number' do
    expect(GoogleImagesSearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
